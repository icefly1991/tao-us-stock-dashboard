# Change Requests

本文件只记录对已接受需求或 ADR 的修改。新功能先写入 `REQUIREMENTS.md`；修正文案或不改变行为的小问题不必建立 CR。

## 状态

`Draft`、`Proposed`、`Accepted`、`Implemented`、`Rejected`、`Deferred`、`Cancelled`。

## 模板

```markdown
## CR-001：标题

- 状态：Accepted
- 提出日期：YYYY-MM-DD
- 决定日期：
- 关联原需求：DATA-xxx / UI-xxx / OPS-xxx
- 关联新需求：
- 关联决策：ADR-xxx

### 原要求

### 新要求

### 修改原因

### 影响分析

- 数据与公式：
- JSON 兼容性：
- 前端与移动端：
- 自动化与部署：
- 测试与文档：

### 风险与回退

### 验收标准

- [ ] ...

### 决定

### 实施记录
```

## 当前记录

首版为独立美股项目，不把参考 A 股仓库的历史需求视为本项目需求。

## CR-001：新增独立的 A/B/C 股票列表

- 状态：Accepted
- 提出及决定日期：2026-09-18
- 关联：DATA-002、DATA-007、UI-002、ADR-007
- 决定依据：用户本次明确要求在原 watchlist 之外展示所提供的另一份列表，沿用相同数据需求。
- 原要求：42 个标的、一个列表。
- 新要求：保留原 42 个标的，新增 100 只股票及 A/B/C 分档；9 只跨列表重合，共 133 个唯一代码。
- 影响：CSV 增加列表归属与分档；JSON 增加可选 collections，各集合包含代码和 Python 生成的分口径汇总。既有字段、指标、数据源、调度、部署方式保持原义；顶层汇总覆盖全部唯一代码。
- 前端：默认原 watchlist，可切换新列表及 A/B/C；失败股票计入列表总数，并明确展示缺失代码。
- 风险：请求规模增大，上游可能缺失/限流；不得替换无法获取的代码。回退可恢复本次修改前的 CSV 和代码。
- 验收：原列表 42、新列表 100、分档 30/35/35；交集不重复下载；汇总和排序随筛选同步；真实数据与规定测试验证。

## CR-002：PSTG 查询映射到 P 与停牌展示

- 状态：Implemented
- 日期：2026-09-18
- 关联：DATA-002、DATA-007
- 确认依据：用户明确回复“pstg cflt你帮我处理吧，停牌的就显示停牌即可”，授权修复查询映射与停牌展示。
- 影响：仅将 PSTG 查询 symbol 改为 P，展示 code 保留 PSTG、名称 Everpure (P)，列表数量与指标不变。
- 回退：symbol=PSTG，继续显示该标的缺失。
- 实施：PSTG 查询 P；CSV 增加 trading_status/status_note，CFLT 显式标记 suspended。JSON 新增可选 suspended 数组，存储 code/name/symbol/note；停牌标的不下载、不生成数值、不计下载失败或涨跌数，仍计入列表总数；页面在榜单末尾显示停牌与“—”。普通下载失败不得推断为停牌；整批无有效行情仍禁止覆盖。

### 核实依据

2026-09-18，首次云端验证 131/133 成功，PSTG、CFLT 无行情。用户指定公司和列表成员不变：根据 [Everpure 官方公告](https://www.everpuredata.com/company/newsroom/press-releases/everpure-to-change-ticker-symbol.html)，PSTG 的同一家公司已改用 P 交易，保留展示 code PSTG，查询 symbol=P，名称 Everpure (P)，用户已确认。这是本次新增标的的已核实查询映射，非替换投资标的或猜测截图。根据 [Nasdaq 公告](https://www.nasdaqtrader.com/TraderNews.aspx?id=ECA2026-164)，CFLT 收购完成后自 2026-03-18 停止交易；保留该用户输入的成员及缺失提示，不替换为 IBM。

## CR-003：吸顶修复、弱势优先排序与独立列表页面

- 状态：Implemented
- 日期：2026-09-18
- 关联：UI-001、UI-002、UI-004
- 确认：用户明确要求修复标题遮挡、下滑吸顶、所有指标表现差者优先、两列表通过链接在不同页面导航。
- 方案：指标切换、榜单标题、列名作为同一正常占位的吸顶区域；表体横向滚动与表头同步。所有指标按数值升序，缺失和停牌末尾；同值按代码排序。原列表 #/watchlist、新列表 #/research，A/B/C 可独立深链接；兼容 Pages 静态刷新和浏览器前后退。
- 影响：仅前端展示与排序，JSON/公式/数据源/部署方式不变。回退本次前端提交即可。
- 验收：桌面与手机首行不被遮挡、页面下滑表头持续可见、横向对齐；每个列表/复权/指标排序正确；链接可直接访问、刷新、前后退，原列表没有 A/B/C 筛选。

## CR-004：持仓调整、快速复制与悬浮 K 线

- 状态：Implemented
- 日期：2026-09-18
- 用户授权：持仓移除 Oscar、增加 EIKN/TTAN，并比照 A 股项目增加快速粘贴和悬浮 K 线速览。
- 关联：DATA-009、UI-005、UI-006、DATA-002
- 列表：OSCR 仅移除持仓归属，仍在观察列表 B 档；TTAN 增加持仓归属；新增 EIKN/Eikon Therapeutics（已核实 Nasdaq/Yahoo 代码）。持仓 43、观察 100，交集 9，共 134 唯一代码（含 CFLT 停牌）。
- 交互：参考项目的快速粘贴指点击名称/代码复制到剪贴板，非导入新股票；悬停代码延迟预览，点击图标固定，手机点按打开，Esc/关闭/外部点击关闭。
- 数据影响：同一次 yfinance 批量请求扩展为最近五个自然年；原榜单公式、250/252 窗口和 YTD 基准不变。新增独立 data/history/{adjusted|raw}/{code}.json：日/周 OHLC、原始成交量（缺失为 null）、历史区间指标、同次生成版本。Open/Volume 只进入新增 K 线契约，既有指标行价格字段不变；行新增可选 history_available。历史不足五年明确提示。
- 复权：K 线 OHLC 全部使用相同 Adj Close/Close 因子，成交量不做复权；周 K 按周一至周日内实际数据聚合。前端只画图，不计算金融指标。
- 新依赖：lightweight-charts 5.2.1（沿用参考项目用于蜡烛与成交量绘制），保留 TradingView attribution；不引入 Tushare、A 股单位或红涨绿跌习惯。
- 失败：单只图表失败不删除有效榜单，明确提示；普通下载失败不伪造 K 线；全批无有效行情继续禁止覆盖。CFLT 只显示停牌，不请求图表。
- 验收：列表成员正确，复制成功/失败降级可见，悬停/固定/关闭/移动端/口径/周日切换及版本一致性测试；真实 yfinance 验证并报告 K 线成功数量。

CR-004 数据质量处理：已验证 Yahoo 的 MNTN/ONON/IOT 含历史 OHLC 矛盾日线。图表不修造价格，跳过并以 skipped_dates 记录、窗口明确提示；受影响周按可用日线聚合，周成交量置 null，避免不完整总量冒充完整周。若最新日异常导致图表最后日期与榜单不同，则图表仍不可用。原榜单计算不变。

## CR-005：仅复制代码与移除已停牌观察成员
- 状态：Implemented
- 授权：2026-09-19 用户要求名称不需要复制按钮、活跃股观察列表去掉停牌股票。
- 关联：UI-007 / DATA-010，覆盖 UI-005 名称复制及 DATA-008 的 CFLT 保留要求。
- 名称改普通文本；代码复制与 K 线不变。CSV 删除仅属观察列表的 CFLT；持仓 43、观察 99（30/34/35）、唯一代码 133。通用停牌能力保留。指标与 JSON 结构不变。

## CR-006：观察池排除中概并补充候选
- 状态：Implemented；关联 DATA-012/RES-003。
- 用户 2026-09-19 授权：移除中概，以后不选；有合适的补至 100，不硬凑。移除七只（含 AOSL 边界从严处理），新增观察八只。名单 100、持仓 43、交集 10，独立下载仍 133。
- 股份名称、ticker 和业务核验见 docs/reviews/2026-09-19-ex-china.md。成员仅 CSV 管理。未知普通行情失败不意味着停牌或中概。无需改金融公式/JSON。


## CR-007：箱体观察原型与市场发现方向
- 状态：Accepted（本地原型）；关联 UI-009 / DATA-013 / RES-004。
- 用户授权：2026-09-19 同意先呈现宽箱体、快速往返、有业务支撑的筛选效果，并明确最终要定期从全网发现，不局限现有100只。
- 新增独立 boxes.json 和 #/boxes；只用复权日线，前60日形成箱体、后60日验证，公式详见 METRIC_DEFINITIONS。原榜单两口径、250/252指标、CSV成员及日更时间均不变。
- 原型先从现有100只真实历史扫描；候选形态与业务核验分离，业务均标待专项复核；不把0匹配改成有推荐。
- 市场范围扩展与网络研究为最终方向，当前未启用自动全市场/全网任务。不声称已部署，不替代用户已有季度复核要求。


## CR-008：手动维护扫描池、一年箱体及四年高位过滤
- 状态：Implemented（本地，尚未发布）；用户本轮明确授权，覆盖 CR-007 的120日箱体及未来定期全市场方向、RES-002季度自动更新计划。
- 箱体扫描固定取活跃股观察列表；用户看到超过30天提示后发起手动更新。名单日期与每日行情更新时间独立，不自动刷新名单日期。
- 最近252条日线扫描箱体，形成/观察各126；上市历史120–251条用实际历史均分并标注不足一年；更少不评分。四年背景按最近四个自然年可用复权日线。
- 原型门槛：当前四年/可用历史区间位置>=80%，或252交易日收益>=100%，或最近252条日线低点反弹>=200%，任一满足排除箱体候选；可在“已排除”查看证据。阈值为本轮实现选择，非用户原话。历史不足明示，不伪称四年完整。
- 不删除活跃名单或改变持仓；原52周指标不变。boxes.json版本升2；新增独立静态名单维护元数据。后续手动更新遵循 docs/ACTIVE_LIST_UPDATE.md。


## CR-009：20%多尺度箱体与完整往返速度
- 状态：Implemented（本地，未发布）；用户认可上一轮方案并明确“执行”。
- 最近一年背景内同时扫描60/90/120/252交易日窗口；每窗前半形成边界，后半固定验证，优先选择有往返证据的近期窗口。宽转窄保留较长窗口对照，不根据最高得分无解释地选框。
- 边界空间下限20%，同时显示底/顶20%内侧区域之间空间；均非实际净利润。
- 完整往返按实际下区→上区→下区日期计自然日，连续循环不重复使用同一单程；保留单程交易日数。<=60自然日优先，61–120次级，>120过慢。当前未完成往返也展示并参与超时判断。
- 至少两轮标已验证，仅一轮标新形成；无完成轮次不视为有效快速箱体。排序兼顾状态、空间/完整周期中位天数、近期位置。四年高位过滤、非中概与业务核查不变。
- boxes.json升v3；仅箱体研究契约变化，原榜单公式、CSV、名单更新时间不变。方案识别而非收益回测，具体实现与验证记录于指标文档。


## CR-010：日线RSI(14)与年内百分位
- 状态：Implemented（本地，未发布）；用户已确认执行上一轮方案。
- 持仓、活跃及箱体列表新增RSI辅助列；主榜单随复权/未复权切换，箱体用复权。用已有收盘价计算Wilder14，不增行情源或依赖。
- 同列显示一位小数RSI、超卖/中性/超买及年内百分位；RSI<=30绿色，>=70红色，中间中性，按显示一位小数分区避免四舍五入后的数值与标签矛盾。
- 百分位为该股票行情日所在自然年有效RSI的含当前日中位秩，非跨股票排名。样本>=20才显示，少于20明确不足；P<=10/P>=90辅助标偏低/偏高，不替代RSI绝对状态。
- 不改变排序、原指标或箱体筛选。dashboard行及boxes行新增可选rsi对象，旧数据缺字段显示缺失；同批箱体复用榜单RSI，防止两处计算来源分叉。boxes维持v3，新增向后兼容字段。


## CR-011：业务标签、两年RSI预览与统一导航
- 状态：Implemented（本地，未发布）；2026-09-19用户明确授权直接实现，关联 UI-014 / DATA-017。
- CSV新增business短标签，透传dashboard、停牌摘要与boxes；不改变成员、排序及指标。
- 按原Wilder14计算完整历史后截取最近两个自然年，history文件新增rsi_history对象；与对应行情版本、复权口径一致。React仅画图。历史不足明示，历史文件失败不阻止榜单显示。
- RSI居中，悬浮/键盘/触屏查看历史；股票身份列横向固定；导航固定持仓股、活跃股观察列表、箱体观察顺序；箱体主标题改宽箱体形态识别。


## CR-012：三个单程一轮、平均四个月与完整形态识别
- 状态：Implemented（本地，未发布）；关联DATA-018/UI-016。
- 用户明确确认三个交替单程为一轮、上下各20%区域触边、只看已完成轮次平均时间。当前未完成耗时及最近单轮均不得成为额外速度剔除条件。
- 用户以AVAV/CRWV说明目标，并要求保留多个窗口、算法贴近肉眼完整形态；在其委托下采用全窗口回顾识别，替代前半校准/后半计数。已向用户说明方法及回顾性边界；具体60/90/120/252窗口、分位边界、严格待观察第三程过半为实现选择，不伪称用户逐字指定。
- 幅度(U/L−1)×100%>=20%，已完成三个单程轮次平均自然日<=120；non-overlapping三程分组，默认仅已验证/待观察，其它原因可查。
- boxes.json升v4，周期/轮次语义变化、前端拒绝旧v3；文档BOX_DEFINITIONS及METRIC_DEFINITIONS同步。原榜单、RSI、名单、每日任务不变。

## CR-013：箱体详情业务、成交量及RSI联动图
- 状态：Implemented（本地，2026-09-20未发布），用户明确要求直接继续；关联UI-017/DATA-019。
- 业务显示在日线详情公司名旁，不加回候选表。成交量沿用同批历史原始volume，RSI复用同批Python全历史计算结果，按日期附加到boxes一年日线；不得在每个短窗口重算RSI。
- 60/90/120/252及一年背景共用时间轴、随窗口同步裁切、悬停/触屏查看同日OHLC/成交量/RSI。缺失显示—，不补0。boxes v4新增可选bars[].rsi_value，不改筛选及排名。
